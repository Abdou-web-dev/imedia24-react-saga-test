import "./App.css";
import { PokemonList } from "./components/PokemonList";

function App() {
  return (
    <div>
      <PokemonList />
    </div>
  ); /* <button onClick={() => api.fetchPokemons()}>fetch pokemons</button> */
}

export default App;
